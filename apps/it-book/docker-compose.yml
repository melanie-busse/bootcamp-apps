bookmonkey-api:
  image: node:20-alpine
  container_name: bookmonkey-api
  # Wir installieren das Paket und starten es explizit auf Host 0.0.0.0
  command: sh -c "npm install -g bookmonkey-api && bookmonkey-api --host 0.0.0.0 --port 3000"
  ports:
    - "3005:3000"
  restart: always